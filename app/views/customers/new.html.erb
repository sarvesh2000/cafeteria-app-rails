<h1>Create New User</h1>
<%= form_with(model: @user, local: true, url: customers_path) do |f| %>
    <%= f.email_field :email %>    
    <%= f.password_field :password %>
    <%= f.submit %>
<% end %>